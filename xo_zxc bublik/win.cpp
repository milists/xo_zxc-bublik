#include "win.h"
#include <string>
#include <iostream>
using namespace std;
string*arr[3][6];
void win() {
	for (int i = 0;i < 6;i++) {
		if (arr[i][0] && 'o') {
			if (arr[i][0] && 'o') {
				if (arr[i][0] && 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[i][2] && 'o') {
			if (arr[i][2] && 'o') {
				if (arr[i][2] && 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[i][5] && 'o') {
			if (arr[i][5] && 'o') {
				if (arr[i][5] && 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[0][i] && 'o') {
			if (arr[0][i] && 'o') {
				if (arr[0][i] && 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[1][i] && 'o') {
			if (arr[1][i] && 'o') {
				if (arr[1][i] && 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[2][i] && 'o') {
			if (arr[2][i] && 'o') {
				if (arr[2][i]&& 'o') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[0][i] && 'o') {
			if (arr[0][i] && 'o') {
				if (arr[0][i] && 'o') {
					if (arr[2][2] && 'o') {
						if (arr[2][i] && 'o') {
							if (arr[2][i] && 'o') {
								if (arr[2][i] && 'o') {

								}
							}
						}
					}

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[i][0] && 'x') {
			if (arr[i][0] && 'x') {
				if (arr[i][0] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[i][2] && 'x') {
			if (arr[i][2] && 'x') {
				if (arr[i][2] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[i][5] && 'x') {
			if (arr[i][5] && 'x') {
				if (arr[i][5] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[0][i] && 'x') {
			if (arr[0][i] && 'x') {
				if (arr[0][i] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[1][i] && 'x') {
			if (arr[1][i] && 'x') {
				if (arr[1][i] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[2][i] && 'x') {
			if (arr[2][i] && 'x') {
				if (arr[2][i] && 'x') {

				}
			}
		}
		cout << "ты победил" << endl;
	}
	for (int i = 0; i < 6; i++) {
		if (arr[0][i] && 'x') {
			if (arr[0][i] && 'x') {
				if (arr[0][i] && 'x') {
					if (arr[2][2] && 'x') {
						if (arr[2][i] && 'x') {
							if (arr[2][i] && 'x') {
								if (arr[2][i] && 'x') {

								}
							}
						}
					}

				}
			}
		}
		cout << "ты победил" << endl;
	}
}